---
// src/components/ui/ServiceCard.astro
import type { HTMLAttributes } from 'astro/types';

interface Props extends HTMLAttributes<'div'> {
  title: string;
  description: string;
  href: string; // URL del enlace "Ver más"
  icon: any; // El componente de icono Astro (ej. PaintBrushIcon)
}

const { title, description, href, icon: IconComponent, class: className, ...attrs } = Astro.props;
---
<div class={`bg-[var(--color-jj-text-light)] px-12 py-10 rounded-lg shadow-md flex flex-col justify-between border border-[var(--color-jj-gray)] ${className}`} {...attrs}>
    {/* Contenedor del Icono y Título */}
    <div class="text-left flex flex-col md:flex-row md:items-center md:flex-wrap mb-4 md:mb-0"> {/* Añadimos mb-4 para móvil, lo quitamos en md */}
        <div class="w-24 h-24 bg-[var(--color-jj-accent)] rounded-full flex items-center justify-center flex-shrink-0 mb-4 md:mb-0 md:mr-5">
            <IconComponent size="w-12 h-12" color="text-[var(--color-jj-dark)]" />
        </div>
        {/* CAMBIO CLAVE AQUÍ PARA EL H3:
           - text-3xl en móvil, text-4xl en MD (responsive typography).
           - mb-4 en móvil (para separarse del párrafo).
           - md:mb-0: ¡Esto es nuevo! En MD (y superior), no queremos que el h3 tenga un margen inferior propio que lo desplace,
                      ya que está en una fila con el icono y `md:items-center` lo centrará.
                      El espaciado del párrafo se manejará en el div del párrafo.
        */}
        <h3 class="text-3xl md:text-4xl font-bold text-[var(--color-jj-dark)] text-left md:flex-grow md:min-w-0 mb-4 md:mb-0" set:html={title}></h3>
    </div>

    {/* Contenedor del Párrafo
       - mb-6 en móvil, md:mb-8 en MD, lg:mb-10 en LG.
       - AÑADIMOS mt-4 md:mt-6 lg:mt-8: Este margen superior en el párrafo es CRUCIAL.
         Ahora que el h3 ya no tiene un mb en MD, el párrafo necesita su propio margen superior
         para separarse del bloque de icono/título.
    */}
    <div class="mt-4 md:mt-6 lg:mt-8 mb-6 md:mb-8 lg:mb-10">
        <p class="text-[var(--color-jj-text-dark)] text-left text-lg md:text-xl">
            {description}
        </p>
    </div>

    {/* Botón "Ver más" */}
    <div class="mt-auto md:mt-0">
        <a href={href} class="bg-[var(--color-jj-dark)] text-[var(--color-jj-text-light)] px-6 py-3 font-semibold hover:opacity-90 transition-opacity block w-full text-center text-lg md:text-xl">
            Ver más
        </a>
    </div>
</div>